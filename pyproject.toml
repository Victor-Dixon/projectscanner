[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "projectscanner"
version = "0.1.0"
description = "Project scanning and repo inventory tooling"
requires-python = ">=3.11"

[tool.setuptools]
py-modules = []
