[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "projectscanner"
version = "0.1.0"
description = "Repo intelligence scanner for Dream.OS portfolios"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.optional-dependencies]
gui = ["PyQt5>=5.15.0"]
dev = ["pytest>=8.0"]
hygiene = [
    "we-are-swarm-agenttools @ git+https://github.com/Victor-Dixon/AgentTools.git@f29f2ab24683e725e1e65f698c5434f04b791313",
]

[project.scripts]
projectscanner = "projectscanner.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]
